{
  "name": "@gp/audit",
  "version": "0.0.1",
  "private": true,
  "description": "Shared GP audit envelope contracts.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=22.17.0"
  }
}
