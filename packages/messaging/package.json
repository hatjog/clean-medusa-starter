{
  "name": "@gp/messaging",
  "version": "0.0.1",
  "private": true,
  "description": "Provider-neutral GP messaging gateway package.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "jest --config ./jest.config.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=22.17.0"
  }
}
