{
  "name": "@gp/messaging",
  "version": "0.0.1",
  "private": true,
  "description": "Provider-neutral GP messaging gateway package.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "jest --config ./jest.config.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "gen:notification-templates": "ts-node --transpile-only ./scripts/generate-notification-template-registry.ts"
  },
  "dependencies": {
    "@gp/audit": "workspace:*",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  },
  "engines": {
    "node": ">=22.17.0"
  }
}
